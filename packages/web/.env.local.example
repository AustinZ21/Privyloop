# PrivyLoop Web – Example .env.local

# Deployment mode: controls enterprise modules on Analytics page
NEXT_PUBLIC_DEPLOYMENT_MODE=open-source

# Global switch for live card overlays (Last scan, Score, Risk badge)
# Set to true in Enterprise to overlay live values on cards
NEXT_PUBLIC_FEATURE_LIVE_CARDS=false

# Server-side enterprise capability gate (prevents client-only toggles)
# The Analytics page checks /api/capabilities which reads this value
ENTERPRISE_ENABLED=false

# AI: Gemini (recommended primary provider)
# Enables real template analysis with Gemini when set; otherwise heuristic fallback is used
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-pro

# Optional: OpenAI fallback provider for AI analysis
OPENAI_API_KEY=
# Default fallback model (user requested 'gpt-5-mini')
OPENAI_MODEL=gpt-5-mini

# Optional: Fallback scraping provider
FIRECRAWL_API_KEY=

# Admin-only token for POST /api/ai/analysis/refresh
# Keep this secret; do not commit
AI_ADMIN_TOKEN=

# Notes
# - For Enterprise, typical settings:
#   NEXT_PUBLIC_DEPLOYMENT_MODE=enterprise
#   NEXT_PUBLIC_FEATURE_LIVE_CARDS=true
#   ENTERPRISE_ENABLED=true
#   GEMINI_API_KEY=sk-...
# - To backfill template analyses:
#   pnpm ai:backfill                # only missing
#   pnpm ai:backfill:all            # recompute all
